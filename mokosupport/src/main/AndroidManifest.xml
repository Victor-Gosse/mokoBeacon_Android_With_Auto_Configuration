<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.moko.support">
    <!-- Créer et supprimer des autorisations de fichiers dans SDCard -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <!-- Écrire l'autorisation de données sur SDCard -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Lorsque requis est vrai à temps, l'application ne peut être installée et exécutée que sur des appareils Android prenant en charge BLE; lorsque requis est faux, les appareils Android peuvent être installés et s'exécuter normalement -->
    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />
    <application
        android:allowBackup="true"
        android:supportsRtl="true">

    </application>

</manifest>
